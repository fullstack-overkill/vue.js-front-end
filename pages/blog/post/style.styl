
@keyframes growd {
  0% { max-height: 900px;  }

  100% { max-height: 300px; }
}

.grows {
  animation: growd 1.5s normal;
  overflow hidden
}

.page-enter-active,
.page-leave-active {
  transition-property: opacity;
  transition-timing-function: ease-in-out;
  transition-duration: 500ms;
}
.page-enter,
.page-leave-to {
  opacity: 0;
}


.page
    display flex
    flex-direction: column
    align-items: center
    min-width 100vw
    min-height: 100vh
    padding: 16px
    .layout-box
        width: 100%
        height: 100%
        margin-top: 75px
        max-width: 701px
        min-height: 590px
        nav 
          a
            display: flex
            align-items: center
            i
              font-size: 8px
              color: #C3C3C3
              margin-right: 10px
            span
              text-align: center
              font-family: 'Helvetica Now Display'
              font-size: 16px
              color: #C3C3C3
              font-weight: 300
              user-select none
          a:hover
            color: #C3C3C3
            text-decoration: none  
        main
          div:nth-child(1)
            h2 
              font-family: 'Playfair Display'
              text-align: left   
              font-size: 20px
              color: #4F5A6B
              font-weight: normal
              user-select none 
              cursor pointer
              text-decoration: underline
              margin: 0
            span
              font-family: 'Playfair Display'
              font-size: 18px
              text-align: left   
              font-size: 10px
              color: #4F5A6B
              font-weight: normal
              user-select none   
          article 
            margin-top: 18px 
            font-size: 18px
              